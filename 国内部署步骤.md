# 国内部署步骤：EdgeOne Pages 前端 + 腾讯云 Docker 后端

本项目建议按前后端分离部署：

- 前端：腾讯云 EdgeOne Pages
- 后端：腾讯云轻量应用服务器或 CVM，通过 Docker Compose 运行
- 数据库：默认使用后端容器里的 SQLite 文件，并挂载到 Docker volume 持久化

## 一、准备工作

需要准备：

- 一个腾讯云账号
- 一个腾讯云轻量应用服务器或 CVM
- 一个可用域名，建议准备两个子域名：
  - `www.example.com`：前端域名，绑定 EdgeOne Pages
  - `api.example.com`：后端 API 域名，解析到腾讯云服务器
- 服务器已安装 Docker 和 Docker Compose

如果暂时没有域名，也可以先用服务器公网 IP 加端口测试：

```text
http://服务器公网IP:3000/health
```

生产环境建议最终使用 HTTPS 域名。

## 二、部署后端到腾讯云服务器

### 1. 登录服务器

在本地终端执行：

```bash
ssh root@你的服务器公网IP
```

### 2. 安装基础工具

如果服务器还没有 Git、Docker，可以先安装。

Ubuntu/Debian 示例：

```bash
apt update
apt install -y git curl
```

Docker 建议按腾讯云或 Docker 官方文档安装。安装完成后确认：

```bash
docker --version
docker compose version
```

### 3. 拉取项目代码

```bash
git clone https://github.com/Spanrua/coffee-shop-schedule.git
cd coffee-shop-schedule
```

如果服务器上已经有项目目录，更新即可：

```bash
cd coffee-shop-schedule
git pull
```

### 4. 创建后端环境变量文件

```bash
cp .env.tencent.example .env
openssl rand -hex 32
```

复制 `openssl` 输出的随机字符串，编辑 `.env`：

```bash
nano .env
```

内容示例：

```env
JWT_SECRET=这里替换成刚才生成的随机字符串
```

注意：`JWT_SECRET` 上线后不要随便改，否则已有登录 token 会失效。

### 5. 启动后端容器

```bash
docker compose -f docker-compose.tencent.yml --env-file .env up -d --build
```

查看容器状态：

```bash
docker compose -f docker-compose.tencent.yml ps
```

查看日志：

```bash
docker compose -f docker-compose.tencent.yml logs -f backend
```

### 6. 测试后端是否可用

在服务器上测试：

```bash
curl http://localhost:3000/health
```

正常返回类似：

```json
{"status":"ok","timestamp":"..."}
```

如果腾讯云安全组临时开放了 `3000` 端口，也可以在本地浏览器访问：

```text
http://服务器公网IP:3000/health
```

生产环境建议只开放 `80/443`，用 Nginx HTTPS 反向代理到 `127.0.0.1:3000`。

## 三、配置后端域名和 HTTPS

建议使用：

```text
api.example.com
```

### 1. DNS 解析

在域名 DNS 控制台添加 A 记录：

```text
主机记录：api
记录类型：A
记录值：腾讯云服务器公网 IP
```

### 2. Nginx 反向代理

如果使用 Nginx，目标是把：

```text
https://api.example.com
```

代理到：

```text
http://127.0.0.1:3000
```

Nginx 示例：

```nginx
server {
    listen 80;
    server_name api.example.com;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

HTTPS 证书可以用腾讯云 SSL 证书、EdgeOne、宝塔面板或 Certbot 配置。

最终后端 API 地址应为：

```text
https://api.example.com/api
```

健康检查地址为：

```text
https://api.example.com/health
```

## 四、部署前端到 EdgeOne Pages

### 1. 进入 EdgeOne Pages

打开腾讯云控制台：

```text
https://console.cloud.tencent.com/edgeone/pages
```

创建 Pages 项目，选择从 Git 仓库导入：

```text
https://github.com/Spanrua/coffee-shop-schedule
```

### 2. 配置构建参数

构建命令：

```bash
cd frontend && npm install && npm run build
```

输出目录：

```text
frontend/dist
```

Node 版本：

```text
18 或 20
```

### 3. 配置环境变量

在 EdgeOne Pages 的环境变量里添加：

```env
VITE_API_URL=https://api.example.com/api
```

把 `https://api.example.com/api` 换成你的真实后端 API 地址。

如果你不配置 `VITE_API_URL`，前端会默认请求 `/api`，这时需要 EdgeOne 的路由代理正确转发 `/api/*` 到后端。

### 4. 检查 EdgeOne 路由配置

仓库里的 `edgeone.config.json` 已包含 SPA 回退配置：

```json
{
  "src": "/(.*)",
  "dest": "/index.html"
}
```

这样刷新 `/login`、`/dashboard` 这类前端路由不会 404。

`/api/*` 的代理目标现在是示例地址：

```json
"dest": "https://api.example.com/api/$1"
```

如果你准备用 EdgeOne 代理 API，需要把这里改成真实后端域名。

如果你已经设置了 `VITE_API_URL=https://api.example.com/api`，前端会直接访问后端域名，EdgeOne 的 `/api/*` 代理不是必须的。

## 五、上线后验证

### 1. 验证后端

```bash
curl https://api.example.com/health
```

应该返回：

```json
{"status":"ok","timestamp":"..."}
```

### 2. 验证前端

打开 EdgeOne Pages 给出的访问地址，确认：

- 页面可以打开
- 刷新页面不 404
- 登录接口请求的地址是 `https://api.example.com/api/...`
- 浏览器控制台没有 CORS 或网络错误

## 六、更新部署

### 后端更新

登录腾讯云服务器：

```bash
cd coffee-shop-schedule
git pull
docker compose -f docker-compose.tencent.yml --env-file .env up -d --build
```

### 前端更新

推送代码到 GitHub：

```bash
git push origin main
```

EdgeOne Pages 会自动重新构建。

## 七、数据备份

当前后端 SQLite 数据库在 Docker volume `coffee-shop-data` 中。

查看 volume：

```bash
docker volume ls
```

建议上线后定期备份该 volume。升级或迁移服务器前，务必先备份数据库。

## 八、常见问题

### 1. 前端能打开，但登录失败

检查 EdgeOne 环境变量：

```env
VITE_API_URL=https://api.example.com/api
```

然后重新部署前端。

### 2. 浏览器提示 CORS 错误

后端当前 `cors()` 是全开放配置，通常不会拦截。更常见原因是：

- API 地址填错
- 后端没启动
- HTTPS 证书或反向代理配置有问题
- 浏览器访问的是 HTTP 后端，前端是 HTTPS，触发 mixed content

### 3. 后端容器启动失败

查看日志：

```bash
docker compose -f docker-compose.tencent.yml logs -f backend
```

常见原因：

- `.env` 没有设置 `JWT_SECRET`
- Docker 没安装完整
- 服务器内存太小导致构建失败

### 4. 数据丢失

确认没有删除 Docker volume：

```bash
docker volume ls
```

不要随意执行：

```bash
docker compose down -v
```

`-v` 会删除 volume，可能导致 SQLite 数据库丢失。
