# Set VITE_API_URL in EdgeOne Pages when the backend has a public HTTPS domain.
# Example: VITE_API_URL=https://api.example.com/api
# If unset, the frontend uses /api and relies on the EdgeOne route proxy.
