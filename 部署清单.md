# 📋 EdgeOne Pages 部署清单

## ✅ 准备工作已完成

### 1. 配置文件
- ✅ `edgeone.config.json` - EdgeOne Pages 配置
- ✅ `frontend/.env.production` - 生产环境变量
- ✅ `frontend/public/_redirects` - SPA 路由重定向
- ✅ `EdgeOne-Pages部署指南.md` - 完整部署文档

### 2. 前端构建
- ✅ 构建命令已执行：`npm run build`
- ✅ 构建产物位置：`frontend/dist/`
- ✅ 产物大小：
  - index.html: 0.45 kB
  - CSS: 27.04 kB (gzip: 5.52 kB)
  - JS: 444.00 kB (gzip: 119.73 kB)

---

## 🚀 EdgeOne Pages 部署步骤

### 方式 1：通过 Git 仓库部署（推荐）⭐

#### 步骤 1：创建 Git 仓库
```bash
cd d:\2026_backend_yjl\coffee-shop-scheduling

# 初始化 Git（如果还没有）
git init

# 添加所有文件
git add .

# 提交
git commit -m "准备部署到 EdgeOne Pages"
```

#### 步骤 2：推送到 GitHub/Gitee
```bash
# GitHub
git remote add origin https://github.com/yourusername/coffee-shop-scheduling.git
git push -u origin main

# 或者 Gitee
git remote add origin https://gitee.com/yourusername/coffee-shop-scheduling.git
git push -u origin main
```

#### 步骤 3：在 EdgeOne Pages 控制台配置

1. **访问控制台**
   - https://console.cloud.tencent.com/edgeone/pages

2. **创建项目**
   - 点击「创建项目」
   - 选择「从 Git 导入」

3. **连接仓库**
   - 授权 GitHub/Gitee
   - 选择 `coffee-shop-scheduling` 仓库

4. **配置构建**
   ```
   框架预设：无
   构建命令：cd frontend && npm install && npm run build
   输出目录：frontend/dist
   安装命令：cd frontend && npm install
   Node 版本：18.x
   ```

5. **环境变量**
   ```
   VITE_API_URL = https://your-backend-domain.com/api
   ```
   ⚠️ **重要**：替换为你的实际后端地址！

6. **部署**
   - 点击「部署」
   - 等待 3-5 分钟
   - 部署完成后获得域名：`your-project.teo.tencentcs.com`

---

### 方式 2：手动上传部署 📦

#### 步骤 1：打包构建产物
```bash
cd d:\2026_backend_yjl\coffee-shop-scheduling\frontend\dist

# Windows: 右键 → 压缩为 ZIP
# 或使用命令行
powershell Compress-Archive -Path * -DestinationPath ../dist.zip
```

#### 步骤 2：上传到 EdgeOne Pages

1. **访问控制台**
   - https://console.cloud.tencent.com/edgeone/pages

2. **创建项目**
   - 点击「创建项目」
   - 选择「手动上传」

3. **上传文件**
   - 上传 `dist.zip`
   - 等待解压和部署

4. **完成**
   - 获得访问域名

---

## 🔧 重要配置项

### 1. 更新后端 API 地址

**文件：`frontend/.env.production`**

将这一行：
```env
VITE_API_URL=https://your-backend-domain.com/api
```

改为你的实际后端地址，例如：
```env
VITE_API_URL=https://api.coffee-shop.com/api
```

### 2. 配置后端 CORS

**文件：`backend/src/server.ts`**

在部署后端后，添加前端域名到 CORS 配置：

```typescript
app.use(cors({
  origin: [
    'http://localhost:5173',
    'https://your-project.teo.tencentcs.com',  // EdgeOne Pages 域名
    'https://coffee-shop.com'                   // 自定义域名
  ],
  credentials: true
}));
```

---

## 🖥️ 后端部署选项

### 选项 1：腾讯云轻量应用服务器（推荐）

**价格**：¥112/年起（2核2G）

**步骤：**
1. 购买服务器：https://console.cloud.tencent.com/lighthouse
2. 安装环境（Node.js, Git, PM2）
3. 克隆代码并启动
4. 配置 Nginx 反向代理
5. 配置 HTTPS（可选）

详细步骤见：[EdgeOne-Pages部署指南.md](EdgeOne-Pages部署指南.md)

### 选项 2：其他云服务

- **Railway**：https://railway.app（支持 SQLite，有免费额度）
- **Render**：https://render.com（免费套餐有限制）
- **Fly.io**：https://fly.io（支持 SQLite）

---

## 📝 部署后检查清单

### 前端检查
- [ ] 访问 EdgeOne Pages 域名
- [ ] 登录页面显示正常
- [ ] 静态资源加载正常（CSS、JS、图标）
- [ ] 浏览器控制台无错误

### 后端连接检查
- [ ] 打开浏览器开发者工具（F12）
- [ ] 尝试登录
- [ ] 检查 Network 标签
- [ ] API 请求是否成功（状态码 200）
- [ ] 是否有 CORS 错误

### 功能测试
- [ ] 管理员登录：admin / admin123
- [ ] 员工登录：emp001 / password123
- [ ] 查看排班
- [ ] 提交请假
- [ ] 打卡功能
- [ ] 通知功能

---

## 🎯 常见问题解决

### 问题 1：前端部署成功，但 API 请求失败

**原因**：后端未部署或 CORS 配置错误

**解决**：
1. 检查 `frontend/.env.production` 中的 API 地址
2. 确保后端已部署并可访问
3. 检查后端 CORS 配置包含前端域名

### 问题 2：页面刷新后 404

**原因**：SPA 路由未配置

**解决**：
- 确保 `frontend/public/_redirects` 文件存在
- EdgeOne Pages 会自动处理

### 问题 3：登录后 token 无法保存

**原因**：跨域 Cookie 设置问题

**解决**：
- 后端 CORS 配置添加 `credentials: true`
- 前端 API 请求添加 `credentials: 'include'`

---

## 💰 预估成本

### EdgeOne Pages（前端）
- **免费额度**：
  - 100GB 流量/月
  - 无限项目
  - 自动 HTTPS
- **价格**：免费（个人项目足够）

### 腾讯云轻量服务器（后端）
- **2核2G，5Mbps**：¥112/年
- **4核8G，8Mbps**：¥288/年

### 域名（可选）
- **.com**：约 ¥55/年
- **.cn**：约 ¥29/年

**最低总成本**：¥112/年

---

## 🔗 快速链接

- **EdgeOne Pages 控制台**：https://console.cloud.tencent.com/edgeone/pages
- **轻量应用服务器**：https://console.cloud.tencent.com/lighthouse
- **域名注册**：https://dnspod.cloud.tencent.com
- **完整部署指南**：[EdgeOne-Pages部署指南.md](EdgeOne-Pages部署指南.md)

---

## 🎉 下一步

1. **选择部署方式**（Git 或手动上传）
2. **部署前端到 EdgeOne Pages**
3. **部署后端到云服务器**
4. **配置环境变量和 CORS**
5. **测试所有功能**
6. **配置自定义域名（可选）**

---

**准备好了吗？开始部署吧！** 🚀

有任何问题随时告诉我！
